# non-blocking-integrity-halt-primitive
A non-blocking integrity halt primitive that suspends execution upon detecting structural integrity violations, returning a non-finalized state instead of refusal or error. Enables safe review, recovery, or resumption prior to semantic execution.
